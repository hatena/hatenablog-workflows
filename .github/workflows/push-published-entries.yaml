name: "[Reusable workflows] push published entries"

on:
  workflow_call:
    inputs:
      runner:
        required: false
        default: ubuntu-latest
        type: string
    secrets:
      OWNER_API_KEY:
        required: true

jobs:
  upload-images:
    if: github.event.pull_request.merged == false
    uses: hatena/hatenablog-workflows/.github/workflows/upload-images.yaml@4733a78ce725116ca961a11a79ab58ccc24a1b1c
    with:
      runner: ${{ inputs.runner }}
    secrets:
      OWNER_API_KEY: ${{ secrets.OWNER_API_KEY }}
